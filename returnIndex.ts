//Jayson Labarrete - typscript 

function returnIndex (simpleArrayOfNums: number[], targetNum = 44):number {
  
 const indexNum =  simpleArrayOfNums.indexOf(targetNum); ;
 return indexNum
}

givenArray = [80,5,9,44,656,1,15,26,7]
const finalAns = returnIndex(givenArray)
console.log(finalAns)

function looping (simpleArrayOfNums: number[], targetNum = 44):number {

// creation of counter
    for (let i=0 ; i < simpleArrayOfNums.length ; i++){
            if (simpleArrayOfNums[i] === targetNum){
                break;
            }

        }
    }
    
// put the counter in a parameter since that will be the index of your targetNum
const finalAns = i 

return finalAns

};


